# aarch64_efi is a UEFI application target; the link uses the linker script
# provided by gnu-efi (elf_aarch64_efi.lds via GNU_EFI_LSCRIPT). This is an
# empty file to appease the Makefile (mirrors hal/x86_64_efi.ld).
